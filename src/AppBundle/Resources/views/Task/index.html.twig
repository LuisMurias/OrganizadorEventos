{% extends 'base_dash.html.twig' %}

{% block content %}
    <div class="row">
        <div class="col-sm-6">
            <h1>Task list</h1>
                <div class="col-sm-12" id="tasklistcontainer">
                {% for tasklist in tasklists %}
                <div class="col-sm-12 tasklist">
                    <h3 class="tasklist_name">{{ tasklist.name }}</h3>
                    <h4 class="tasklist_description">{{ tasklist.description }}</h4>
                    <div class="col-sm-offset-1 col-sm-11 tasks">
                        {% for task in tasklist.tasks %}
                            <div class="task">
                                <input type="checkbox" class="chkTask" data-taskid="{{ task.id }}" />
                                <h3 class="task_name">{{ task.name }}</h3>
                                <h4 class="task_description">{{ task.description }}</h4>
                            </div>

                        {% endfor %}
                    </div>
                </div>
                {% endfor %}
            </div>
        </div>
        <div class="col-sm-6">
            <h1>Task list</h1>
        </div>
    </div>
{% endblock %}
{% block javascripts %}
<script src="{{ asset('js/mv/task.js') }}"></script>
{% endblock %}